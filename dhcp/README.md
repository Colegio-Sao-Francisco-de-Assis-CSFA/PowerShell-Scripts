# Scripts da categoria `dhcp`

Lista de scripts disponíveis nesta categoria:

- `add-dhcp-reservation.ps1` — Adiciona reservas DHCP com base em um CSV.
- `get-dhcpleases.ps1` — Obtém os leases ativos do servidor DHCP.
- `set-dhcpremote-machine.ps1` — Define configurações de rede via DHCP remotamente.
